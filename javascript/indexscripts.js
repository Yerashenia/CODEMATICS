function updateProgressBarMaths(progress) {
    var progressBar = document.getElementById('my-progress-bar-maths');
    progressBar.style.width = progress + '%';
  }

  

function updateProgressBarCode(progress) {
    var progressBar = document.getElementById('my-progress-bar-code');
    progressBar.style.width = progress + '%';
  }

  updateProgressBarMaths(34);
  updateProgressBarCode(82);

